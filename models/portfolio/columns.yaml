# ===================================
#  List Column Definitions
# ===================================

columns:
    worker_business_name:
        label: Business Name
        searchable: true
        relation: worker
        select: business_name

    user_name:
        label: Name
        relation: user
        select: name
        searchable: true

    user_email:
        label: Email
        relation: user
        select: email
        searchable: true
