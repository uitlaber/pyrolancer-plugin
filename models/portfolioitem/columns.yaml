# ===================================
#  List Column Definitions
# ===================================

columns:
    is_primary:
        label: Primary item
        type: switch

    type:
        label: Type
        relation: type
        select: name

    title:
        label: Title
