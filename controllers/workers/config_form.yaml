# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Worker

# Model Form Field configuration
form: ~/plugins/responsiv/pyrolancer/models/worker/fields.yaml

# Model Class name
modelClass: Responsiv\Pyrolancer\Models\Worker

# Default redirect location
defaultRedirect: responsiv/pyrolancer/workers

# Create page
create:
    title: Create Worker
    redirect: responsiv/pyrolancer/workers/preview/:id
    redirectClose: responsiv/pyrolancer/workers/preview/:id

# Update page
update:
    title: Edit Worker
    redirect: responsiv/pyrolancer/workers/preview/:id
    redirectClose: responsiv/pyrolancer/workers/preview/:id

# Preview page
preview:
    title: Preview Worker